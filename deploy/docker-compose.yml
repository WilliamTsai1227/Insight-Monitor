services:
  monitor:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8001:8001"
    env_file:
      - ../backend/.env
    environment:
      - QDRANT_HOST=${QDRANT_HOST:-qdrant}
    restart: unless-stopped
